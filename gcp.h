#ifndef GCP_H
#define GCP_H

#include "graph.h"

int first_fit(Graph G);

int welsh_powel(Graph G);

int ldo(Graph G);

int ido(Graph G);

int dsatur(Graph G);

int rlf(Graph G);

#endif